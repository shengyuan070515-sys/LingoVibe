# 每日阅读 · UI 改版 — 对话决策记录

**效力**：以对话结论为准；实现阶段与 [2026-04-04-daily-reading-featured-decision-log](./2026-04-04-daily-reading-featured-decision-log.md) 等产品决策并列参考。  
**状态**：已锁定（含纯中文选区 **②**）。

---

## 已锁定

| # | 主题 | 结论 |
|---|------|------|
| 1 | 改版范围 | **A + C**：**精选外刊卡片区** + **「每日阅读」整页**信息架构；反对所有功能竖向挤在同一长页。 |
| 2 | 导航结构 | **① 页内 Tabs**，**同一 URL**，不拆子路由。 |
| 3 | Tab 与默认 | 四个 Tab（顺序）：**今日精选** → **联网搜索** → **导入文本** → **我的书库**；**默认「今日精选」**。 |
| 4 | 我的书库展示 | 与「今日精选」一致，采用 **卡片栅格**，不再以当前「列表行」为主展示。 |
| 5 | 书库卡片字段 | **A 标题 + B 难度 + C 正文/摘要预览 + D 来源类型 + E 时间**；**难度放在角标**（五档中文：入门～高阶）。时间用绝对日期或相对时间 **实现阶段再细化**。 |
| 6 | Tab 条与栅格 | **① 顶栏横向 Tab**（可横向滑动）；栅格 **a**：窄屏 **1 列**，`sm` 及以上 **2 列**（今日精选与书库一致）。 |
| 7 | 划选交互（阅读器） | **划选单词或句子**后出现 **上下文操作**，而非仅依赖顶栏按钮：**单词** → 提供 **翻译**、**加入生词本**；翻译 → 弹层样式参考 **「视觉词典」单词卡**（含语境例句等）。**句子/多词** → **翻译**（**仅展示中文**）、**语法分析**。 |
| 8 | 阅读器顶栏按钮 | **B**：**移除**与「当前选区」强绑定的按钮（如翻译选中、语法分析选中、选中加入生词本等）；**选区能力一律由划选菜单承担**。顶栏（或后续吸顶条）**保留全文级能力**（如翻译全文、朗读全文等，具体列表实现阶段与现网对齐）。 |
| 9 | 全文工具条（吸顶） | **吸顶**：向下滚动长文时，**全文类**操作条 **固定在视口顶部**（或紧贴标题下的窄条），避免滚回页顶。同时 **尽量少占纵向空间**：实现上倾向 **单行、紧凑内边距**；窄屏可 **仅图标或图标+极简文案**，具体样式实现阶段定稿。 |
| 10 | 单词 vs 句子判定 | **② 阈值 + 可预期规则**：选区 **trim** 后按空白切 **token**；**英文词形**允许内部 **`'`、`’`、连字符 `-`** 视为同一词。若 **恰好 1 个英文 token** 且长度 ≤ **48** → **单词菜单**（翻译卡 + 生词本）；**≥2 个英文 token** → **句子菜单**（仅中文译 + 语法）。**纯中文/无英文 token**：见 **决策 13**（仅提示，不调 LLM）。 |
| 11 | Jina 元数据行 | **A**：阅读器 **默认完全不展示** `Title:` / `URL Source:` / `Markdown Content:` 等 **调试感** 行；正文区只呈现 **文章化** 内容。页面仍保留 **大标题** 与 **「原文出处」**（canonical URL）。**不**提供「原始抓取信息」折叠（与 B 相比更干净、实现更省）。 |
| 12 | 单词卡数据与组件 | **① 一次 LLM**：用每日阅读 Key **结构化**拉取释义、音标或近似音、**1 条英例句 + 中文译**（例句尽量贴合 **当前文章语境**）；**发音**用浏览器 **SpeechSynthesis**（`en-US` 等），不强制额外购买发音 API。**划选弹层**与 **生词本单词卡** **必须共用同一套 UI 组件**（**内容区块与样式一致**；**外形容器**允许不同 **尺寸/比例**）。若生词本尚无卡片，以 **阅读器弹层为首版规范**，再扩展到生词本。**入库规则**：仅当用户 **明确点击「加入生词本」** 才写入生词本；**仅查看划选翻译卡片** 时 **不自动** 加入生词本。 |
| 13 | 纯中文选区（决策 10 补充） | **②**：选区为 **纯中文**（无有效英文 token）时 **不调用** 翻译/语法 LLM；仅 **轻量提示** 用户 **「请划选英文单词或句子」**（文案实现时可微调）。 |

---

## 待决议

（暂无；若后续改版可再开新表。）

---

## 修订历史

- **2026-04-05**：初版；锁定决策 1–8（含选区菜单与顶栏 **B**）。  
- **2026-04-05**：锁定决策 9（全文工具条 **吸顶** + **低占用** 约束）。  
- **2026-04-05**：锁定决策 10（划选 **单词 / 句子** 判定规则）。  
- **2026-04-05**：锁定决策 11（正文 **隐藏 Jina 元数据行**，**A**）。  
- **2026-04-05**：锁定决策 12（单词卡 **① LLM** + **共用 UI 组件** + **不自动入库**）。  
- **2026-04-05**：锁定决策 13（**纯中文选区 → 仅提示，不调 LLM**，**②**）。
